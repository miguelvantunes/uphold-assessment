# Uphold Assessement - Currency Converter made with Vite + React

## Setup Project:

Create a .env based on the .env.example file.

You can use the same values of both keys.

VITE_UPHOLD_CLIENT_ID=foo

VITE_UPHOLD_CLIENT_SECRET=bar

## Run the project:

```
npm install
npm run dev
```
